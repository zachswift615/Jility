# Ngrok configuration for external testing
# Use with: ./dev.sh start --config=.env.ngrok

# Use relative /api path (Next.js will proxy to backend)
NEXT_PUBLIC_API_URL=/api

# Backend URL for Next.js server-side proxy
BACKEND_URL=http://localhost:3900

# Add any other ngrok-specific settings here
